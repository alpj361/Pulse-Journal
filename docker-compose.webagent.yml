services:
  webagent:
    build: ./WebAgent
    container_name: webagent
    env_file:
      - ./WebAgent/.env
    environment:
      - PORT=8787
    ports:
      - "8787:8787"
    shm_size: 2g
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp=unconfined
